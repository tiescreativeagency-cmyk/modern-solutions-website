# Optional full HTTPS URL to `hero-bg.gif` on a CDN (otherwise `/hero-bg.gif` from `public/` is used).
# NEXT_PUBLIC_HERO_GIF_URL=

# Regenerate GIF from your MP4 (edit path in scripts/make-hero-gif.mjs or pass as arg):
#   npm run hero-gif -- /path/to/video.mp4
