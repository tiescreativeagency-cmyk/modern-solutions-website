# Optional full HTTPS URL to your hero MP4 (e.g. Vercel Blob, S3, CloudFront).
# If unset, the site uses `/hero-video-default.mp4` from `public/` — commit that file so Vercel can serve it.
# NEXT_PUBLIC_HERO_VIDEO_URL=
